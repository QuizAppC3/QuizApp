<!-- selection.html.erb -->
<div class="container">
  <h1>Ausgewählte Kategorien</h1>

  <% if @selected_categories.present? %>
    <p>Du hast folgende Kategorien ausgewählt:</p>
    <div class="category-boxes">
      <% @selected_categories.each do |kategorie| %>
        <div class="category-box"><%= kategorie %></div>
      <% end %>
    </div>
  <% else %>
    <p>Du hast keine Kategorien ausgewählt.</p>
  <% end %>

  <div class="button-group">
    <%= link_to 'Zurück zur Auswahl', categories_index_path, class: 'btn btn-back' %>
    <%= link_to 'Weiter', game_next_question_path, class: 'btn btn-next' %>
  </div>
</div>
