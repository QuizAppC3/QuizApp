<h1>Spiel beendet!</h1>

<p>Dein Endstand: <strong><%= @game.score %> Punkte</strong></p>

<% if @game.score >= (session[:total_questions] * 10 / 2)%>
  <p>🎉 Glückwunsch! Super gemacht! 🎉</p>
<% else %>
  <p>Gut gespielt – beim nächsten Mal wird’s besser!</p>
<% end %>

<div style="margin-top: 2rem;">
  <%= link_to 'Neues Spiel starten', root_path, style: "display: inline-block; padding: 1rem 2rem; font-size: 1rem; background-color: #3498db; color: white; border: none; border-radius: 8px; text-decoration: none; cursor: pointer; transition: background-color 0.3s;" %>
</div>
